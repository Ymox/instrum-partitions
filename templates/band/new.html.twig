{% extends app.request.isXmlHttpRequest() ? 'base_ajax.html.twig': 'base.html.twig' %}

{% block title %}{{ 'app.band.new.title'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
	{% if app.request.xmlHttpRequest %}
	<div class="modal-header">
		<h2 class="modal-title">{{ 'app.band.new.heading'|trans }}</h2>
		<button type="button" class="close" data-dismiss="modal" aria-label="{{ 'global.action.close' }}">
		  <span aria-hidden="true">×</span>
		</button>
	</div>
	<div class="modal-body">
	{% else %}
	<h1>{{ 'app.band.new.heading'|trans }}</h1>
	{% endif %}

	{{ form_start(form, {'attr': {'class': app.request.xmlHttpRequest ? 'ajaxify' : null}}) }}
		{{ form_widget(form) }}
		<button band="submit" class="btn btn-primary float-left"><i class="fa fa-check"></i></button>
	{{ form_end(form) }}
	{% if not app.request.isXmlHttpRequest() %}
	<a href="{{ path('band_index') }}" class="btn btn-light float-right"><i class="fa fa-list"></i></a>
	{% endif %}
	
	{% if app.request.isXmlHttpRequest() %}
	</div>
	{% endif %}
{% endblock %}
