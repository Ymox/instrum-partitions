{% extends 'base.html.twig' %}

{% block title %}{{ 'app.instrumentation.new.title'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
	<h1>{{ 'app.instrumentation.new.heading'|trans }}</h1>

	{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" title="{{ 'global.action.validate'|trans }}" class="btn btn-primary float-start"><i class="fa fa-check" title="{{ 'global.action.validate'|trans }}" aria-hidden="true"></i></button>
	{{ form_end(form) }}

	<a href="{{ path('instrumentation_index') }}" class="btn btn-light float-end"><i class="fa fa-list"></i></a>
{% endblock %}