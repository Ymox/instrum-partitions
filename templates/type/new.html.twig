{% extends 'base.html.twig' %}

{% block title %}{{ 'app.type.new.title'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
	<h1>{{ 'app.type.new.heading'|trans }}</h1>

	{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class="btn btn-primary float-left"><i class="fa fa-check"></i></button>
	{{ form_end(form) }}

	<a href="{{ path('type_index') }}" class="btn btn-light float-right"><i class="fa fa-list"></i></a>
{% endblock %}
