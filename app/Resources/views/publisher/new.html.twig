{% extends 'base.html.twig' %}

{% block title %}{{ 'ysoft.instrum.publisher.new.title'|trans }}{% endblock %}

{% block body %}
	<h1>{{ 'ysoft.instrum.publisher.new.heading'|trans }}</h1>

	{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class="btn btn-primary float-left"><i class="fa fa-check"></i></button>
	{{ form_end(form) }}

	<a href="{{ path('publisher_index') }}" class="btn btn-light float-right"><i class="fa fa-list"></i></a>
{% endblock %}