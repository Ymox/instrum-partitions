{% extends 'base.html.twig' %}

{% block title %}{{ 'ysoft.instrum.type.new.title'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
	<h1>{{ 'ysoft.instrum.type.new.heading'|trans }}</h1>

	{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class="btn btn-primary float-left"><i class="fa fa-check"></i></button>
	{{ form_end(form) }}

	<a href="{{ path('type_index') }}" class="btn btn-secondary float-right"><i class="fa fa-list"></i></a>
{% endblock %}
