{% extends 'base.html.twig' %}

{% block title %}{{ 'ysoft.instrum.instrumentation.new.title'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
	<h1>{{ 'ysoft.instrum.instrumentation.new.heading'|trans }}</h1>

	{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class="btn btn-primary float-left"><i class="fa fa-check"></i></button>
	{{ form_end(form) }}
	
	<a href="{{ path('instrumentation_index') }}" class="btn btn-light float-right"><i class="fa fa-list"></i></a>
{% endblock %}