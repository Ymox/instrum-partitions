{% extends 'base.html.twig' %}

{% block title %}{{ 'ysoft.instrum.status.new.title'|trans }} | {{ parent() }}{% endblock %}

{% block body %}
	<h1>{{ 'ysoft.instrum.status.new.heading'|trans }}</h1>

	{{ form_start(form) }}
		{{ form_widget(form) }}
		<button type="submit" class="btn btn-primary"><i class="fa fa-check"></i></button>
	{{ form_end(form) }}

	<a href="{{ path('status_index') }}" class="btn btn-secondary"><i class="fa fa-list"></i></a>
{% endblock %}
